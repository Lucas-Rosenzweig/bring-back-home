[project]
name = "bring-back-home-radio"
version = "0.1.0"
description = "Nintendo Switch LDN discovery and active client experiments"
requires-python = ">=3.13"
dependencies = [
    "pycryptodome>=3.23,<4",
    "python-netlink>=0.0.15,<0.1",
    "trio>=0.30,<1",
]

[tool.uv]
package = false
